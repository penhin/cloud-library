<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.maijianda.cloud_library.mapper.BookMapper">

    <resultMap id="BookResultMap" type="com.maijianda.cloud_library.entity.Book">
        <id property="bookId" column="book_id"/>
        <result property="title" column="title"/>
        <result property="author" column="author"/>
        <result property="publisher" column="publisher"/>
        <result property="totalStock" column="total_stock"/>
        <result property="availableStock" column="available_stock"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="bookId">
        INSERT INTO books (title, author, publisher, total_stock, available_stock)
        VALUES (#{title}, #{author}, #{publisher}, #{totalStock}, #{availableStock})
    </insert>

    <update id="update">
        UPDATE books
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="author != null">author = #{author},</if>
            <if test="publisher != null">publisher = #{publisher},</if>
            <if test="totalStock != null">total_stock = #{totalStock},</if>
            <if test="availableStock != null">available_stock = #{availableStock}</if>
        </set>
        WHERE book_id = #{bookId}
    </update>

    <delete id="deleteById">
        DELETE FROM books WHERE book_id = #{bookId}
    </delete>

    <select id="selectById" resultMap="BookResultMap">
        SELECT * FROM books WHERE book_id = #{bookId}
    </select>

    <select id="selectByConditions" resultMap="BookResultMap">
        SELECT * FROM books
        <where>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="author != null and author != ''">
                AND author LIKE CONCAT('%', #{author}, '%')
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>
